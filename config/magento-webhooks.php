<?php

return [

    /* Determines which queue jobs should be dispatched on. */
    'queue' => 'default',

    /* List of events to dispatch. */
    'events' => [
        //
    ],

    /* Prefix for the webhook route. */
    'prefix' => 'magento',

    /* Middleware for all the routes. */
    'middleware' => [
        //
    ],

];
